# Client — everything here ships inside the app bundle. PUBLIC values only.
VITE_SUPABASE_URL=https://jqpeohbbbmnoujxaiutr.supabase.co
# Must start with sb_publishable_ (or the legacy anon JWT). NEVER sb_secret_.
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxx
VITE_API_URL=http://localhost:8787
# Google Cloud Console → Credentials → OAuth client ID → "Web application"
VITE_GOOGLE_WEB_CLIENT_ID=xxxxxxxx.apps.googleusercontent.com
