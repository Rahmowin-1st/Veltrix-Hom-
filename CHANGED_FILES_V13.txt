VELTRIX HOM V13 — CHANGED FILES
Baseline: veltrix-hom-v12-vercel-ts2322-fixed.zip

=== NEW ===
src/components/ui/NoteWriteIcon.tsx
    Note-page + pencil vector icon for the empty composer action.
    Rounded paper corners, stroke-based, currentColor, ferrule cross-stroke
    so it does not read as an arrow. Replaces the diagonal PencilLine.

=== MODIFIED ===
src/screens/General.tsx
    Removed the duplicate `v5-hero-composer` markup and General's second
    createVoiceInput controller (two recognizers, one microphone).
    Now renders the shared <ChatComposer variant="hero" />.

src/components/chat/ChatComposer.tsx
    Added `variant?: 'chat' | 'hero'` (spacing only — no structural change),
    emitted as data-variant. Swapped PencilLine for NoteWriteIcon.

src/components/shell/SettingsDrawer.tsx
    Deleted PRIMARY_SHORTCUTS (General/Manbalar/Personal) — a duplicate of the
    bottom navigation. Manbalar folded into the quick-tools rail.
    Account header and the home/return control merged into one top row;
    the return control is now a compact circle, not a full-width list item.
    Tools rail renders dimensional tiles with semantic tones.

src/styles/v5.css
    + hero composer variant
    + drawer top row and compact return control
    + selective 3D tool tiles (one gradient + three box-shadows, shared light
      direction, semantic tones, reduced-motion aware)
    + matching 3D on the two primary composer actions

=== UNCHANGED (verified by diff -rq) ===
server/**            entire tree, byte-for-byte identical
migrations           008, 010, 011, 012-rpc-overload-hotfix + server/src/db/*
All V12 hotfix docs  RENDER_SUPABASE_FIX, RPC_OVERLOAD_HOTFIX, STUCK_PROCESSING_FIX

=== DATABASE ===
No database migration required.
