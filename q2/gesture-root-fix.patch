--- a/app/src/main/kotlin/com/veltrix/calculator/app/MainActivity.kt
+++ b/app/src/main/kotlin/com/veltrix/calculator/app/MainActivity.kt
@@ -209,12 +209,14 @@
 
         displayColumn.addView(buildTopActions(), LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, dp(58f)))
+        val brainGesture = InteriorBrainGesture()
         val display = LinearLayout(this).apply {
             tag = "calculator_display"
             orientation = LinearLayout.VERTICAL
             gravity = Gravity.BOTTOM or Gravity.END
             setPadding(dp(10f), dp(12f), dp(8f), dp(10f))
-            setOnTouchListener(InteriorBrainGesture())
+            isClickable = true
+            setOnTouchListener(brainGesture)
         }
         val expressionLayer = FrameLayout(this).apply { tag = "standard_expression" }
 standardExpressionView = NoImeEditText(this).apply {
@@ -227,6 +229,7 @@
     contentDescription = "Expression"
     setSelectAllOnFocus(false)
     setSelection(text.length)
+    setOnTouchListener(brainGesture)
     addTextChangedListener(object : android.text.TextWatcher {
         override fun beforeTextChanged(s: CharSequence?, start: Int, count: Int, after: Int) = Unit
         override fun onTextChanged(s: CharSequence?, start: Int, before: Int, count: Int) {
@@ -249,6 +252,8 @@
     maxLines = 2
     setTextIsSelectable(true)
     contentDescription = "Result"
+    isClickable = true
+    setOnTouchListener(brainGesture)
 }
 resultLayer.addView(standardResultView, FrameLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, ViewGroup.LayoutParams.WRAP_CONTENT, Gravity.END))
 display.addView(expressionLayer, LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, ViewGroup.LayoutParams.WRAP_CONTENT))
@@ -610,7 +615,10 @@
                     tracking = false
                     velocityTracker?.recycle()
                     velocityTracker = VelocityTracker.obtain().also { it.addMovement(event) }
-                    return true
+                    // Keep ordinary expression/result taps working. The touched child (or the
+                    // clickable display itself) owns the stream, while this shared listener sees
+                    // subsequent MOVE events and takes over only after a horizontal drag intent.
+                    return false
                 }
                 MotionEvent.ACTION_MOVE -> {
                     velocityTracker?.addMovement(event)
