 
     private var runtimeShader: RuntimeShader? = null
@@ -60,46 +61,58 @@ class GlassMaterialRenderer(private val owner: View) {
             float2 p = local - center;
             float2 halfSize = max(center - float2(1.0), float2(1.0));
             float d = sdRoundRect(p, halfSize, radius);
-            float edgeBand = max(3.0, 5.0 + thickness * 5.0);
+            float edgeBand = max(4.0, 7.0 + thickness * 6.0);
             float edge = smoothstep(-edgeBand, 0.0, d);
-            float2 n = normalize(p / max(halfSize, float2(1.0)) + float2(0.0001));
+            float interior = 1.0 - edge;
+            float2 normalizedP = p / max(halfSize, float2(1.0));
+            float radial = clamp(length(normalizedP), 0.0, 1.4);
+            float2 n = normalize(normalizedP + float2(0.0001));
 
             float2 uv = sceneOrigin + local;
-            float magnify = (1.0 - edge) * (0.008 + thickness * 0.010);
-            float2 centerWarp = -p * magnify;
-            float2 edgeWarp = n * edge * edge * (2.4 + 5.6 * thickness + 2.8 * press);
+            // Convex-lens center magnification and a stronger edge refraction band.
+            float lens = (0.020 + thickness * 0.020) * interior * (1.0 - 0.32 * radial);
+            float2 centerWarp = -p * lens;
+            float edgePower = edge * edge * (4.4 + 8.6 * thickness + 3.2 * press);
+            float2 edgeWarp = n * edgePower;
 
             float2 touchP = touch - center;
             float maxDim = max(size.x, size.y);
             float td = length(p - touchP) / max(maxDim, 1.0);
-            float touchEnergy = exp(-td * td * 12.0) * press;
+            float touchEnergy = exp(-td * td * 14.0) * press;
             float2 touchDir = normalize((p - touchP) + float2(0.001));
-            float2 touchWarp = touchDir * touchEnergy * (1.2 + thickness * 2.5);
+            float2 touchWarp = touchDir * touchEnergy * (2.0 + thickness * 3.8);
 
             float2 sampleCoord = uv + centerWarp + edgeWarp + touchWarp;
             half4 base = scene.eval(sampleCoord);
 
-            float chroma = 0.30 + 0.62 * thickness;
+            float chroma = 0.48 + 0.95 * thickness + 0.35 * press;
             half4 cR = scene.eval(sampleCoord + n * chroma);
             half4 cB = scene.eval(sampleCoord - n * chroma);
             half3 transmitted = half3(cR.r, base.g, cB.b);
 
+            // A second offset sample provides subtle internal scatter without blur passes.
+            half3 softSample = scene.eval(sampleCoord - n * (1.2 + thickness * 1.6)).rgb;
+            transmitted = mix(transmitted, softSample, half(0.08 + (1.0 - clarity) * 0.12));
+
             half luminance = dot(transmitted, half3(0.2126, 0.7152, 0.0722));
-            half3 adaptiveTint = half3(tint.rgb) * half(0.82 + 0.25 * float(luminance));
+            half3 adaptiveTint = half3(tint.rgb) * half(0.80 + 0.28 * float(luminance));
             half3 color = mix(transmitted, adaptiveTint, half(tintStrength));
 
-            float scatter = 0.025 + (1.0 - clarity) * 0.09 + press * 0.018;
+            float scatter = 0.010 + (1.0 - clarity) * 0.055 + press * 0.010;
             color = mix(color, half3(1.0), half(scatter));
 
-            float2 lightDir = normalize(float2(-0.58, -0.82));
-            float hi = edge * max(0.0, dot(n, lightDir));
-            float low = edge * max(0.0, dot(n, -lightDir));
-            float touchLight = touchEnergy * (0.08 + edge * 0.18);
-            color += half3(hi * (0.20 + thickness * 0.18) + touchLight);
-            color -= half3(low * (0.045 + thickness * 0.07));
-
-            float alpha = 0.82 + 0.12 * clarity + 0.035 * thickness;
-            return half4(clamp(color, half3(0.0), half3(1.0)), half(clamp(alpha, 0.0, 1.0)));
+            float2 lightDir = normalize(float2(-0.62, -0.78));
+            float hi = edge * pow(max(0.0, dot(n, lightDir)), 1.7);
+            float low = edge * pow(max(0.0, dot(n, -lightDir)), 1.35);
+            float caustic = edge * (0.08 + 0.24 * thickness) * (0.55 + 0.45 * (1.0 - radial));
+            float touchLight = touchEnergy * (0.10 + edge * 0.24);
+            color += half3(hi * (0.30 + thickness * 0.22) + caustic + touchLight);
+            color -= half3(low * (0.07 + thickness * 0.10));
+
+            // Lower opacity preserves a stronger real-scene read while the warped sample supplies
+            // the actual visible material. Tinted actions remain transparent rather than flat.
+            float alpha = 0.63 + 0.10 * clarity + 0.055 * thickness + 0.035 * press;
+            return half4(clamp(color, half3(0.0), half3(1.0)), half(clamp(alpha, 0.0, 0.88)));
         }
     """.trimIndent()
 
@@ -136,23 +149,25 @@ class GlassMaterialRenderer(private val owner: View) {
             val originY = (ownerLocation[1] - hostLocation[1]).toFloat() + bounds.top
 
             val thickness = when (variant) {
-                Variant.HIGH_CLARITY -> 0.62f
-                Variant.TINTED_ACTION -> 0.88f
-                Variant.THICK_EXPANDED -> 1.35f
-                Variant.LIGHTWEIGHT -> 0.42f
-                Variant.REGULAR_CLEAR -> 0.76f
+                Variant.HIGH_CLARITY -> 0.84f
+                Variant.TINTED_ACTION -> 1.02f
+                Variant.THICK_EXPANDED -> 1.46f
+                Variant.LIGHTWEIGHT -> 0.34f
+                Variant.REGULAR_CLEAR -> 0.90f
             }
             val clarity = when (variant) {
-                Variant.HIGH_CLARITY -> 0.96f
-                Variant.THICK_EXPANDED -> 0.72f
-                Variant.LIGHTWEIGHT -> 0.88f
-                else -> 0.90f
+                Variant.HIGH_CLARITY -> 0.98f
+                Variant.THICK_EXPANDED -> 0.76f
+                Variant.LIGHTWEIGHT -> 0.90f
+                Variant.TINTED_ACTION -> 0.92f
+                else -> 0.95f
             }
             val tintStrength = when (variant) {
-                Variant.TINTED_ACTION -> 0.27f
-                Variant.THICK_EXPANDED -> 0.07f
-                Variant.LIGHTWEIGHT -> 0.035f
-                else -> 0.045f
+                Variant.TINTED_ACTION -> 0.22f
+                Variant.THICK_EXPANDED -> 0.06f
+                Variant.LIGHTWEIGHT -> 0.018f
+                Variant.HIGH_CLARITY -> 0.018f
+                else -> 0.025f
             }
             val r = Color.red(tintColor) / 255f
             val g = Color.green(tintColor) / 255f
@@ -178,11 +193,21 @@ class GlassMaterialRenderer(private val owner: View) {
     }
 
     private fun drawOpticalRim(canvas: Canvas, bounds: RectF, radius: Float, press: Float, thickness: Float) {
-        rimPaint.strokeWidth = max(1f, owner.resources.displayMetrics.density * (0.7f + thickness * 0.25f))
-        rimPaint.color = Color.argb((82 + press * 38).toInt().coerceIn(0, 140), 255, 255, 255)
+        val density = owner.resources.displayMetrics.density
+        rimPaint.strokeWidth = max(1f, density * (0.75f + thickness * 0.30f))
+        rimPaint.color = Color.argb((112 + press * 42).toInt().coerceIn(0, 170), 255, 255, 255)
         fallbackRect.set(bounds)
-        fallbackRect.inset(rimPaint.strokeWidth * 0.55f, rimPaint.strokeWidth * 0.55f)
+        fallbackRect.inset(rimPaint.strokeWidth * 0.62f, rimPaint.strokeWidth * 0.62f)
         canvas.drawRoundRect(fallbackRect, max(0f, radius - rimPaint.strokeWidth), max(0f, radius - rimPaint.strokeWidth), rimPaint)
+
+        shadePaint.strokeWidth = max(1f, density * (0.8f + thickness * 0.24f))
+        shadePaint.color = Color.argb((34 + thickness * 12f).toInt().coerceIn(0, 62), 53, 65, 76)
+        fallbackRect.set(bounds)
+        fallbackRect.inset(shadePaint.strokeWidth * 1.05f, shadePaint.strokeWidth * 1.05f)
+        canvas.save()
+        canvas.clipRect(bounds.left, bounds.centerY(), bounds.right, bounds.bottom)
+        canvas.drawRoundRect(fallbackRect, max(0f, radius - shadePaint.strokeWidth), max(0f, radius - shadePaint.strokeWidth), shadePaint)
+        canvas.restore()
     }
 
     private fun drawFallback(
 diff --git a/q2base/VeltrixCalculator/app/src/main/kotlin/com/veltrix/calculator/app/frontend/glass/GlassSceneHost.kt b/q2work/VeltrixCalculator/app/src/main/kotlin/com/veltrix/calculator/app/frontend/glass/GlassSceneHost.kt
index aee3e13..54ec1db 100644
--- a/q2base/VeltrixCalculator/app/src/main/kotlin/com/veltrix/calculator/app/frontend/glass/GlassSceneHost.kt
+++ b/q2work/VeltrixCalculator/app/src/main/kotlin/com/veltrix/calculator/app/frontend/glass/GlassSceneHost.kt
@@ -13,13 +13,10 @@ import android.widget.FrameLayout
 import kotlin.math.max
 
 /**
- * A controlled scene host for Veltrix optical controls.
+ * Shared light-scene sampler for Veltrix optical material.
  *
- * The host keeps one reusable snapshot of the real child content layer. Glass views are
- * omitted while that snapshot is recorded, so they can refract the content underneath
- * without recursively sampling themselves. The snapshot is refreshed only when explicitly
- * invalidated or when the host size changes; interaction-only glass animation does not cause
- * a full-scene copy every frame.
+ * Scene capture is explicit and bounded: interaction-only glass animation reuses the same bitmap;
+ * screen/content changes call invalidateScene(). The bitmap is recycled only on size change.
  */
 class GlassSceneHost @JvmOverloads constructor(
     context: Context,
@@ -35,9 +32,11 @@ class GlassSceneHost @JvmOverloads constructor(
 
     private val basePaint = Paint(Paint.ANTI_ALIAS_FLAG)
     private val fieldPaint = Paint(Paint.ANTI_ALIAS_FLAG)
+    private val structurePaint = Paint(Paint.ANTI_ALIAS_FLAG).apply { style = Paint.Style.STROKE }
     private var baseGradient: Shader? = null
     private var fieldA: Shader? = null
     private var fieldB: Shader? = null
+    private var fieldC: Shader? = null
 
     val sceneGeneration: Long get() = generationInternal
     val sceneSnapshot: Bitmap? get() = sceneBitmap
@@ -59,30 +58,27 @@ class GlassSceneHost @JvmOverloads constructor(
         sceneBitmap?.recycle()
         sceneBitmap = Bitmap.createBitmap(w, h, Bitmap.Config.ARGB_8888)
         sceneCanvas = Canvas(sceneBitmap!!)
+
         baseGradient = LinearGradient(
-            0f,
-            0f,
-            w.toFloat(),
-            h.toFloat(),
-            Color.rgb(251, 251, 248),
-            Color.rgb(246, 249, 252),
+            0f, 0f, w.toFloat(), h.toFloat(),
+            intArrayOf(Color.rgb(252, 251, 247), Color.rgb(247, 249, 251), Color.rgb(250, 249, 246)),
+            floatArrayOf(0f, 0.56f, 1f),
             Shader.TileMode.CLAMP,
         )
         fieldA = RadialGradient(
-            w * 0.18f,
-            h * 0.20f,
-            max(w, h) * 0.64f,
-            intArrayOf(Color.rgb(255, 247, 229), Color.argb(0, 255, 247, 229)),
-            floatArrayOf(0f, 1f),
-            Shader.TileMode.CLAMP,
+            w * 0.14f, h * 0.15f, max(w, h) * 0.46f,
+            intArrayOf(Color.rgb(255, 236, 196), Color.argb(0, 255, 236, 196)),
+            floatArrayOf(0f, 1f), Shader.TileMode.CLAMP,
         )
         fieldB = RadialGradient(
-            w * 0.86f,
-            h * 0.58f,
-            max(w, h) * 0.58f,
-            intArrayOf(Color.rgb(228, 241, 255), Color.argb(0, 228, 241, 255)),
-            floatArrayOf(0f, 1f),
-            Shader.TileMode.CLAMP,
+            w * 0.88f, h * 0.42f, max(w, h) * 0.50f,
+            intArrayOf(Color.rgb(218, 238, 255), Color.argb(0, 218, 238, 255)),
+            floatArrayOf(0f, 1f), Shader.TileMode.CLAMP,
+        )
+        fieldC = RadialGradient(
+            w * 0.48f, h * 0.92f, max(w, h) * 0.38f,
+            intArrayOf(Color.rgb(239, 232, 250), Color.argb(0, 239, 232, 250)),
+            floatArrayOf(0f, 1f), Shader.TileMode.CLAMP,
         )
         dirty = true
     }
@@ -98,14 +94,31 @@ class GlassSceneHost @JvmOverloads constructor(
         canvas.drawRect(0f, 0f, width.toFloat(), height.toFloat(), fieldPaint)
         fieldPaint.shader = fieldB
         canvas.drawRect(0f, 0f, width.toFloat(), height.toFloat(), fieldPaint)
+        fieldPaint.shader = fieldC
+        canvas.drawRect(0f, 0f, width.toFloat(), height.toFloat(), fieldPaint)
         basePaint.shader = null
         fieldPaint.shader = null
+
+        // Calm structured scene information gives refraction something real to bend without
+        // turning the calculator into wallpaper. Geometry is cached-by-size and allocation-free.
+        val d = resources.displayMetrics.density
+        structurePaint.strokeWidth = 0.75f * d
+        structurePaint.color = Color.argb(23, 92, 112, 132)
+        val step = 52f * d
+        var x = -width.toFloat()
+        while (x < width * 2f) {
+            canvas.drawLine(x, 0f, x + height * 0.32f, height.toFloat(), structurePaint)
+            x += step
+        }
+        structurePaint.strokeWidth = 1.0f * d
+        structurePaint.color = Color.argb(20, 207, 144, 54)
+        val cy = height * 0.22f
+        canvas.drawCircle(width * 0.82f, cy, 78f * d, structurePaint)
+        canvas.drawCircle(width * 0.82f, cy, 106f * d, structurePaint)
     }
 
     override fun dispatchDraw(canvas: Canvas) {
-        if (dirty && !isCapturingScene && width > 0 && height > 0) {
-            updateSceneSnapshot()
-        }
+        if (dirty && !isCapturingScene && width > 0 && height > 0) updateSceneSnapshot()
         super.dispatchDraw(canvas)
     }
 
