diff --git a/q2work/VeltrixCalculator/app/src/androidTest/kotlin/com/veltrix/calculator/app/FrontendQ2QualityRuntimeTest.kt b/q2work/VeltrixCalculator/app/src/androidTest/kotlin/com/veltrix/calculator/app/FrontendQ2QualityRuntimeTest.kt
new file mode 100644
index 0000000..75e0b5f
--- /dev/null
+++ b/q2work/VeltrixCalculator/app/src/androidTest/kotlin/com/veltrix/calculator/app/FrontendQ2QualityRuntimeTest.kt
@@ -0,0 +1,80 @@
+package com.veltrix.calculator.app
+
+import android.view.MotionEvent
+import android.view.View
+import android.view.ViewGroup
+import androidx.test.core.app.ActivityScenario
+import androidx.test.ext.junit.runners.AndroidJUnit4
+import com.veltrix.calculator.app.frontend.NoImeEditText
+import org.junit.Assert.*
+import org.junit.Test
+import org.junit.runner.RunWith
+
+@RunWith(AndroidJUnit4::class)
+class FrontendQ2QualityRuntimeTest {
+    private fun findByTag(root: View, tag: String): View? {
+        if (root.tag == tag) return root
+        if (root is ViewGroup) for (i in 0 until root.childCount) findByTag(root.getChildAt(i), tag)?.let { return it }
+        return null
+    }
+
+    private fun findByDescription(root: View, description: String): View? {
+        if (root.contentDescription?.toString() == description) return root
+        if (root is ViewGroup) for (i in 0 until root.childCount) findByDescription(root.getChildAt(i), description)?.let { return it }
+        return null
+    }
+
+    @Test fun aStandardAndDirectBrainGesture() {
+        ActivityScenario.launch(MainActivity::class.java).use { scenario ->
+            scenario.onActivity { activity ->
+                val root = activity.window.decorView
+                val display = findByTag(root, "calculator_display")!!
+                assertNotNull(findByTag(root, "standard_expression"))
+                assertNotNull(findByTag(root, "standard_result"))
+                val y = display.height.coerceAtLeast(120) * 0.55f
+                val startX = display.width.coerceAtLeast(240) * 0.82f
+                val endX = display.width.coerceAtLeast(240) * 0.15f
+                val t = android.os.SystemClock.uptimeMillis()
+                display.dispatchTouchEvent(MotionEvent.obtain(t, t, MotionEvent.ACTION_DOWN, startX, y, 0))
+                display.dispatchTouchEvent(MotionEvent.obtain(t, t + 30, MotionEvent.ACTION_MOVE, (startX + endX) * 0.55f, y, 0))
+                display.dispatchTouchEvent(MotionEvent.obtain(t, t + 60, MotionEvent.ACTION_MOVE, endX, y, 0))
+                display.dispatchTouchEvent(MotionEvent.obtain(t, t + 80, MotionEvent.ACTION_UP, endX, y, 0))
+                assertNotEquals(View.INVISIBLE, findByTag(root, "main_brain")!!.visibility)
+            }
+        }
+    }
+
+    @Test fun bLibraryUsesOneLensAndNoImeSearch() {
+        ActivityScenario.launch(MainActivity::class.java).use { scenario ->
+            scenario.onActivity { activity ->
+                val root = activity.window.decorView
+                findByDescription(root, "Library")!!.performClick()
+            }
+            Thread.sleep(250)
+            scenario.onActivity { activity ->
+                val root = activity.window.decorView
+                assertNotNull(findByTag(root, "library_subject_lens"))
+                val search = findByTag(root, "library_mega_search") as NoImeEditText
+                assertFalse(search.showSoftInputOnFocus)
+                search.requestFocus()
+                search.setText("biyt")
+                assertEquals("biyt", search.text.toString())
+            }
+        }
+    }
+
+    @Test fun cConvertersHaveDedicatedCurrencyEntry() {
+        ActivityScenario.launch(MainActivity::class.java).use { scenario ->
+            scenario.onActivity { activity ->
+                val root = activity.window.decorView
+                findByDescription(root, "Converters")!!.performClick()
+            }
+            Thread.sleep(200)
+            scenario.onActivity { activity ->
+                val root = activity.window.decorView
+                assertNotNull(findByTag(root, "converters_screen"))
+                assertNotNull(findByDescription(root, "Currency"))
+            }
+        }
+    }
+}
diff --git a/q2base/VeltrixCalculator/app/src/main/kotlin/com/veltrix/calculator/app/MainActivity.kt b/q2work/VeltrixCalculator/app/src/main/kotlin/com/veltrix/calculator/app/MainActivity.kt
index 204c5ca..4ef313d 100644
--- a/q2base/VeltrixCalculator/app/src/main/kotlin/com/veltrix/calculator/app/MainActivity.kt
+++ b/q2work/VeltrixCalculator/app/src/main/kotlin/com/veltrix/calculator/app/MainActivity.kt
@@ -17,6 +17,7 @@ import android.view.Gravity
 import android.view.HapticFeedbackConstants
 import android.view.MotionEvent
 import android.view.View
+import android.view.VelocityTracker
 import android.view.ViewGroup
 import android.view.WindowInsets
 import android.view.WindowInsetsController
@@ -63,6 +64,9 @@ class MainActivity : Activity() {
     private var reducedMotion = false
     private var hapticsEnabled = true
     private var brainOpen = false
+    private enum class BrainTransitionState { CLOSED, DRAGGING, SETTLING_OPEN, OPEN, SETTLING_CLOSED }
+    private var brainTransitionState = BrainTransitionState.CLOSED
+    private var brainProgress = 0f
     private var lastCurrencyActiveNetworkAt = 0L
 
     override fun onCreate(savedInstanceState: Bundle?) {
@@ -469,96 +473,234 @@ display.addView(Button(this).apply {
     // -----------------------------------------------------------------------------------------
 
     private fun buildMainBrain(): FrameLayout {
-        val root = FrameLayout(this).apply { tag = "main_brain"; setBackgroundColor(Color.argb(247, 249, 249, 246)) }
+        val root = FrameLayout(this).apply {
+            tag = "main_brain"
+            setBackgroundColor(Color.TRANSPARENT)
+        }
         val scroll = ScrollView(this).apply { overScrollMode = View.OVER_SCROLL_NEVER }
-        val content = LinearLayout(this).apply { orientation = LinearLayout.VERTICAL; setPadding(dp(20f), dp(18f), dp(20f), dp(32f)) }
+        val content = LinearLayout(this).apply {
+            orientation = LinearLayout.VERTICAL
+            setPadding(dp(20f), dp(16f), dp(20f), dp(30f))
+        }
         scroll.addView(content)
         root.addView(scroll, FrameLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, ViewGroup.LayoutParams.MATCH_PARENT))
 
         val top = LinearLayout(this).apply { orientation = LinearLayout.HORIZONTAL; gravity = Gravity.CENTER_VERTICAL }
         top.addView(TextView(this).apply {
             text = "VELTRIX"
-            textSize = 25f
+            textSize = 24f
             typeface = Typeface.create("sans", Typeface.BOLD)
             setTextColor(VeltrixTokens.Ink)
-            letterSpacing = 0.08f
+            letterSpacing = 0.09f
         }, LinearLayout.LayoutParams(0, dp(58f), 1f))
         top.addView(glassIcon(LiquidGlassButton.Glyph.CLOSE, "Close Main Brain", { closeBrain() }, dp(54f)))
         content.addView(top)
-        content.addView(label("Main Brain", 13f, VeltrixTokens.Muted).apply { setPadding(0, 0, 0, dp(18f)) })
-
-        content.addView(brainAction("Calculator", "Standard Calculator") { closeBrain(false); showScreen("standard") })
-        content.addView(sectionLabel("Last Used 5"))
-        val lastUsed = LinearLayout(this).apply { tag = "brain_last_used_5"; orientation = LinearLayout.VERTICAL }
-        content.addView(lastUsed)
-
-        content.addView(sectionLabel("Converters"))
-        content.addView(brainAction("Converters", "Fast unit and currency conversion") { closeBrain(false); showScreen("converters") })
-        content.addView(sectionLabel("Library"))
-        content.addView(brainAction("Library", "Browse all specialized environments") { closeBrain(false); showScreen("library") })
-        content.addView(brainAction("Graphs", "Professional graph environment") { closeBrain(false); showScreen("graph") })
-        content.addView(brainAction("History", "Saved calculation trail") { closeBrain(false); showScreen("history") })
-        content.addView(brainAction("Widgets", "Home-screen calculator tools") { closeBrain(false); showScreen("widgets") })
-        content.addView(brainAction("Settings", "Precision, motion and accessibility") { closeBrain(false); showScreen("settings") })
+        content.addView(label("Control space", 12f, VeltrixTokens.Muted).apply { setPadding(dp(2f), 0, 0, dp(14f)) })
+
+        content.addView(LiquidGlassButton(this).apply {
+            label = "Calculator"
+            contentDescription = "Calculator"
+            variant = GlassMaterialRenderer.Variant.HIGH_CLARITY
+            tintColor = VeltrixTokens.Accent
+            reducedTransparency = this@MainActivity.reducedTransparency
+            hapticsEnabled = this@MainActivity.hapticsEnabled
+            setOnClickListener { closeBrain(false); showScreen("standard") }
+        }, LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, dp(66f)).apply { bottomMargin = dp(16f) })
+
+        content.addView(sectionLabel("LAST USED 5"))
+        val lastScroll = HorizontalScrollView(this).apply {
+            isHorizontalScrollBarEnabled = false
+            overScrollMode = View.OVER_SCROLL_NEVER
+        }
+        val lastUsed = LinearLayout(this).apply {
+            tag = "brain_last_used_5"
+            orientation = LinearLayout.HORIZONTAL
+        }
+        lastScroll.addView(lastUsed)
+        content.addView(lastScroll, LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, dp(68f)).apply { bottomMargin = dp(14f) })
+
+        content.addView(sectionLabel("EXPLORE"))
+        val primaryRow = LinearLayout(this).apply { orientation = LinearLayout.HORIZONTAL }
+        primaryRow.addView(brainGlassAction("Converters") { closeBrain(false); showScreen("converters") }, LinearLayout.LayoutParams(0, dp(72f), 1f).apply { rightMargin = dp(6f) })
+        primaryRow.addView(brainGlassAction("Library") { closeBrain(false); showScreen("library") }, LinearLayout.LayoutParams(0, dp(72f), 1f).apply { leftMargin = dp(6f) })
+        content.addView(primaryRow, LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, dp(72f)).apply { bottomMargin = dp(16f) })
+
+        val secondary = GridLayout(this).apply {
+            columnCount = 2
+            alignmentMode = GridLayout.ALIGN_BOUNDS
+            useDefaultMargins = false
+        }
+        listOf(
+            "Graphs" to { closeBrain(false); showScreen("graph") },
+            "History" to { closeBrain(false); showScreen("history") },
+            "Widgets" to { closeBrain(false); showScreen("widgets") },
+            "Settings" to { closeBrain(false); showScreen("settings") },
+        ).forEachIndexed { index, item ->
+            val b = VeltrixSurfaceButton(this).apply {
+                text = item.first
+                textSize = 15f
+                quiet = true
+                contentDescription = item.first
+                setOnClickListener { item.second.invoke() }
+            }
+            val lp = GridLayout.LayoutParams().apply {
+                width = 0
+                height = dp(54f)
+                columnSpec = GridLayout.spec(index % 2, 1f)
+                rowSpec = GridLayout.spec(index / 2)
+                setMargins(if (index % 2 == 0) 0 else dp(5f), dp(4f), if (index % 2 == 0) dp(5f) else 0, dp(4f))
+            }
+            secondary.addView(b, lp)
+        }
+        content.addView(secondary, LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, ViewGroup.LayoutParams.WRAP_CONTENT))
         return root
     }
 
+    private fun brainGlassAction(title: String, click: () -> Unit): View = LiquidGlassButton(this).apply {
+        label = title
+        contentDescription = title
+        variant = GlassMaterialRenderer.Variant.REGULAR_CLEAR
+        tintColor = VeltrixTokens.Accent
+        reducedTransparency = this@MainActivity.reducedTransparency
+        hapticsEnabled = this@MainActivity.hapticsEnabled
+        setOnClickListener { click() }
+    }
+
     private fun refreshBrainAdaptive() {
         val list = brainPanel.findViewWithTag<View>("brain_last_used_5") as? LinearLayout ?: return
         list.removeAllViews()
         val state = adaptive.load()
         val ids = AdaptiveEngine.lastUsed5(state)
         if (ids.isEmpty()) {
-            list.addView(label("Specialized tools you use will appear here.", 14f, VeltrixTokens.Muted).apply { setPadding(dp(2f), dp(4f), 0, dp(8f)) })
+            list.addView(label("Specialized tools you use appear here.", 14f, VeltrixTokens.Muted).apply {
+                gravity = Gravity.CENTER_VERTICAL
+                setPadding(dp(2f), 0, dp(16f), 0)
+            }, LinearLayout.LayoutParams(ViewGroup.LayoutParams.WRAP_CONTENT, dp(60f)))
         } else {
             ids.forEach { id ->
                 val tool = platform.registry.get(id) ?: return@forEach
-                list.addView(brainAction(tool.title, "${tool.subject.wireName} • ${tool.topic}") { closeBrain(false); openTool(tool) })
+                list.addView(VeltrixSurfaceButton(this).apply {
+                    text = tool.title
+                    textSize = 13f
+                    quiet = true
+                    maxLines = 1
+                    contentDescription = "Recent ${tool.title}"
+                    setOnClickListener { closeBrain(false); openTool(tool) }
+                }, LinearLayout.LayoutParams(dp(148f), dp(58f)).apply { rightMargin = dp(8f) })
             }
         }
     }
+
+    private fun applyBrainProgress(progress: Float) {
+        val p = progress.coerceIn(0f, 1f)
+        brainProgress = p
+        val travel = brainPanel.width.takeIf { it > 0 }?.toFloat() ?: resources.displayMetrics.widthPixels.toFloat()
+        brainPanel.translationX = travel * (1f - p)
+        brainPanel.alpha = 0.76f + 0.24f * p
+        stage.translationX = -dp(22f) * p
+        stage.scaleX = 1f - 0.012f * p
+        stage.scaleY = 1f - 0.012f * p
+    }
 
     private fun openBrain() {
-        if (brainOpen) return
+        if (brainTransitionState == BrainTransitionState.OPEN || brainTransitionState == BrainTransitionState.SETTLING_OPEN) return
         refreshBrainAdaptive()
         brainOpen = true
+        brainTransitionState = BrainTransitionState.SETTLING_OPEN
         brainPanel.visibility = View.VISIBLE
+        brainPanel.animate().cancel()
+        stage.animate().cancel()
         brainPanel.post {
             if (reducedMotion) {
-                brainPanel.translationX = 0f; brainPanel.alpha = 1f
+                applyBrainProgress(1f)
+                brainTransitionState = BrainTransitionState.OPEN
             } else {
-                brainPanel.translationX = brainPanel.width * 0.18f
-                brainPanel.alpha = 0.72f
-                brainPanel.animate().cancel()
-                brainPanel.animate().translationX(0f).alpha(1f).setDuration(220L).start()
+                if (brainProgress <= 0.001f) applyBrainProgress(0f)
+                brainPanel.animate().translationX(0f).alpha(1f).setDuration(190L).withEndAction {
+                    brainProgress = 1f
+                    brainTransitionState = BrainTransitionState.OPEN
+                }.start()
+                stage.animate().translationX(-dp(22f).toFloat()).scaleX(0.988f).scaleY(0.988f).setDuration(190L).start()
             }
             shell.invalidateScene()
         }
     }
 
     private fun closeBrain(animate: Boolean = true) {
-        if (!brainOpen) return
+        if (!brainOpen && brainTransitionState == BrainTransitionState.CLOSED) return
         brainOpen = false
+        brainTransitionState = BrainTransitionState.SETTLING_CLOSED
+        brainPanel.animate().cancel()
+        stage.animate().cancel()
         if (!animate || reducedMotion) {
-            brainPanel.visibility = View.INVISIBLE; brainPanel.translationX = 0f; brainPanel.alpha = 1f
+            applyBrainProgress(0f)
+            brainPanel.visibility = View.INVISIBLE
+            stage.translationX = 0f; stage.scaleX = 1f; stage.scaleY = 1f
+            brainTransitionState = BrainTransitionState.CLOSED
         } else {
-            brainPanel.animate().cancel()
-            brainPanel.animate().translationX(brainPanel.width * 0.16f).alpha(0f).setDuration(170L).withEndAction {
-                brainPanel.visibility = View.INVISIBLE; brainPanel.translationX = 0f; brainPanel.alpha = 1f
+            val travel = brainPanel.width.takeIf { it > 0 }?.toFloat() ?: resources.displayMetrics.widthPixels.toFloat()
+            brainPanel.animate().translationX(travel).alpha(0.76f).setDuration(170L).withEndAction {
+                brainPanel.visibility = View.INVISIBLE
+                brainProgress = 0f
+                brainTransitionState = BrainTransitionState.CLOSED
             }.start()
+            stage.animate().translationX(0f).scaleX(1f).scaleY(1f).setDuration(170L).start()
         }
     }
 
     private inner class InteriorBrainGesture : View.OnTouchListener {
         private var downX = 0f
         private var downY = 0f
+        private var tracking = false
+        private var velocityTracker: VelocityTracker? = null
+
         override fun onTouch(v: View, event: MotionEvent): Boolean {
             when (event.actionMasked) {
-                MotionEvent.ACTION_DOWN -> { downX = event.x; downY = event.y }
-                MotionEvent.ACTION_UP -> {
+                MotionEvent.ACTION_DOWN -> {
+                    if (brainOpen || event.x < dp(24f) || event.x > v.width - dp(24f)) return false
+                    downX = event.x
+                    downY = event.y
+                    tracking = false
+                    velocityTracker?.recycle()
+                    velocityTracker = VelocityTracker.obtain().also { it.addMovement(event) }
+                    return true
+                }
+                MotionEvent.ACTION_MOVE -> {
+                    velocityTracker?.addMovement(event)
                     val dx = event.x - downX
                     val dy = event.y - downY
-                    if (dx < -dp(68f) && abs(dx) > abs(dy) * 1.4f) { openBrain(); return true }
+                    if (!tracking && abs(dx) > dp(8f) && abs(dx) > abs(dy) * 1.2f) {
+                        if (dx > 0) return false
+                        tracking = true
+                        refreshBrainAdaptive()
+                        brainOpen = true
+                        brainTransitionState = BrainTransitionState.DRAGGING
+                        brainPanel.visibility = View.VISIBLE
+                        brainPanel.animate().cancel(); stage.animate().cancel()
+                    }
+                    if (tracking) {
+                        val travel = (v.width * 0.78f).coerceAtLeast(dp(120f).toFloat())
+                        applyBrainProgress((-dx / travel).coerceIn(0f, 1f))
+                        return true
+                    }
+                }
+                MotionEvent.ACTION_UP, MotionEvent.ACTION_CANCEL -> {
+                    velocityTracker?.addMovement(event)
+                    velocityTracker?.computeCurrentVelocity(1000)
+                    val vx = velocityTracker?.xVelocity ?: 0f
+                    velocityTracker?.recycle(); velocityTracker = null
+                    if (!tracking) return false
+                    val complete = event.actionMasked == MotionEvent.ACTION_UP && (brainProgress > 0.42f || vx < -dp(620f))
+                    tracking = false
+                    if (complete) {
+                        brainTransitionState = BrainTransitionState.DRAGGING
+                        openBrain()
+                    } else {
+                        brainOpen = true
+                        brainTransitionState = BrainTransitionState.DRAGGING
+                        closeBrain(true)
+                    }
+                    return true
                 }
             }
             return false
         }
     }
