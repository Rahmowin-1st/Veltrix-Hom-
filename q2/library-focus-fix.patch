--- a/app/src/main/kotlin/com/veltrix/calculator/app/MainActivity.kt
+++ b/app/src/main/kotlin/com/veltrix/calculator/app/MainActivity.kt
@@ -780,6 +780,10 @@
 
         val keyboard = buildAlphaKeyboard(search) { renderLibrary() }.apply { visibility = View.GONE }
+        // buildAlphaKeyboard installs its own focus listener. Re-attach Library visibility after
+        // construction so focusing Mega Search reliably exposes the custom keyboard.
+        search.setOnFocusChangeListener { _, focused ->
+            if (focused) keyboard.visibility = View.VISIBLE
+        }
         root.addView(keyboard, LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, dp(205f)))
         libraryUi = LibraryUi(list, search, lens, subjectsScroll, keyboard)
         renderLibrary()
