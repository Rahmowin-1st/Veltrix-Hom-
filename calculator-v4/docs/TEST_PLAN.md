# Backend Master Verification Plan

Final CI must run core unit/property tests; Android app + androidTest compilation; deterministic AVD creation/list/boot; ADB connection; APK install; cold launch/resumed MainActivity; Standard + Scientific + Programmer; Registry/Search; representative subject tools; converter; real live currency probe then provider/cache/offline failure path; OCR representative path; widget direct calculation; graph; history/personalization/widget process persistence; Activity recreation; offline deterministic calculation; connected instrumentation; package verification; final force-stop/reinstall/relaunch; exact source/APK SHA-256 provenance.

Any stage that does not execute is NOT VERIFIED. OEM-specific floating-window behavior remains NOT VERIFIED unless a suitable physical OEM runtime is actually tested.
