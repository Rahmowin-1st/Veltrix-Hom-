VELTRIX HOM V15 — CHANGED FILES
Baseline: veltrix-hom-v14-chat-final.zip

=== NEW (4) ===
src/components/chat/ChatSearch.tsx      Find-in-chat: in-memory search, next/prev, wrap,
                                        diacritic + apostrophe insensitive, registers as
                                        an overlay so Back closes it first
src/lib/blocksToText.ts                 Shared block→plain-text flattener used by BOTH
                                        Copy and search, so what is visible is findable
                                        and copyable
src/lib/drawerGesture.ts                Pure gesture decisions: resolveAxis (direction
                                        lock), shouldSnapOpen (velocity + hysteresis),
                                        progressFromDx, drawerTravel
src/lib/__tests__/drawerGesture.test.ts 12 tests
src/lib/__tests__/chatRetry.test.ts     13 tests

=== MODIFIED (5) ===
src/screens/Chat.tsx
    + requestSnapshots: frozen inputs per assistant turn (text, attachment,
      sourceIds, talentId, translation) → true retry replay
    + send(replaceTurnId, replay): regenerate swaps content in place, adds no
      user turn, preserves slot id, clears stale feedback
    + regenerate() uses a NEW request id; failure-path retry still reuses its id
    + floating top controls (menu / new-chat + overflow)
    + find-in-chat, overflow menu, real chat file list
    + ResizeObserver-measured composer height → exact tail spacer
    + gesture: shared snap logic, multi-touch abort, pointercancel/blur/
      visibilitychange cleanup

src/components/chat/Message.tsx
    + Turn.regenerating, Turn.feedback
    + action row: Copy, Like, Dislike, Voice, Retry
    + single module-level TTS owner (no overlapping speech)
    + regenerating dims the old answer instead of blanking it
    - local blocksToText (moved to src/lib/blocksToText.ts)

src/components/chat/ChatMenu.tsx
    + Uploaded files view (real attachments, empty state, download)
    + Find in chat entry
    + files/onFindInChat props — entries hidden where data is unavailable

src/components/shell/SettingsDrawer.tsx
    close-drag now uses the same shouldSnapOpen() as the edge-open gesture

src/styles/v5.css
    + floating top control islands (mixed white/light-blue, no blur)
    + find-in-chat bar, search-hit flash
    + menu file list + count
    + composer absolutely positioned so content passes behind it
    + regenerating state
    ~ chat scroll bottom padding removed (spacer owns clearance now)

=== UNCHANGED (verified by diff -rq) ===
server/**  byte-for-byte identical. All migrations, RPCs, worker, auth untouched.

=== DATABASE ===
No migration required.
