/** Real service limits, shared by the client so no screen invents its own. */
export const MAX_PDF_MB = 20
export const MAX_PDF_BYTES = MAX_PDF_MB * 1024 * 1024
export const MAX_TRANSLATE_MB = 20
export const MAX_TRANSLATE_BYTES = MAX_TRANSLATE_MB * 1024 * 1024
export const SOURCE_NAME_MAX = 15
